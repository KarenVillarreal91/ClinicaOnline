import { PresionarDirective } from './presionar.directive';

describe('PresionarDirective', () => {
  it('should create an instance', () => {
    const directive = new PresionarDirective();
    expect(directive).toBeTruthy();
  });
});
