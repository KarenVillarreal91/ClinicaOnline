import { OscurecerDirective } from './oscurecer.directive';

describe('OscurecerDirective', () => {
  it('should create an instance', () => {
    const directive = new OscurecerDirective();
    expect(directive).toBeTruthy();
  });
});
