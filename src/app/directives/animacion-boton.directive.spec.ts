import { AnimacionBotonDirective } from './animacion-boton.directive';

describe('AnimacionBotonDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimacionBotonDirective();
    expect(directive).toBeTruthy();
  });
});
